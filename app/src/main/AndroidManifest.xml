<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="smart.pro.invoice">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <meta-data
        android:name="android.support.VERSION"
        android:value="25.3.1" />
    <meta-data
        android:name="smart.rua.hostel.app.MyAppGlideModule"
        android:value="GlideModule" />

    <application
        android:name="smart.pro.invoice.app.AppController"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="smart.pro.invoice.SplashActivity"
            android:theme="@style/AppThemeCommon"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="smart.pro.invoice.MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name="smart.pro.invoice.invoice.InvoiceListActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

        </activity>
        <activity android:name="smart.pro.invoice.seller.SellerListActivity" />
        <activity android:name="smart.pro.invoice.buyer.BuyerListActivity" />
        <activity android:name="smart.pro.invoice.MainEditActivity" />
        <activity android:name="smart.pro.invoice.CustomerReport" />
        <activity android:name="smart.pro.invoice.ChangePassword" />
        <activity android:name="smart.pro.invoice.SettingsActivity" />

        <provider
            android:name="smart.pro.invoice.app.GenericFileProvider"
            android:authorities="smart.pro.invoice.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>